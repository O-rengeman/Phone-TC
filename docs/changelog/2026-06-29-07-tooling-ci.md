# 🧱 CI・テスト・依存・Lint 整備

- **日付**: 2026-06-29
- **種別**: 基盤整備（コミット時点のスナップショットを記録）
- **対象コミット**: `2b0f55c`
- **主な対象ファイル**:
  - 新規 `.github/workflows/ci.yml`
  - 新規 `vitest.config.ts`
  - 変更 `package.json`, `package-lock.json`, `eslint.config.js`, `.gitignore`, `index.html`
  - 新規 `.continue/agents/*.yaml`（エディタ設定）

## 変更内容

- **テスト基盤**: `vitest.config.ts` を追加（jsdom 環境・カバレッジ）。`package.json` に `test` / `test:run` / `test:cov` スクリプト。devDependencies に `vitest`, `@vitest/coverage-v8`, `jsdom` 等。
- **CI**: `.github/workflows/ci.yml` を追加（lint / typecheck / test の自動実行）。
- **依存**: アプリ依存に `smpte-timecode`, `peerjs`, `qrcode.react`, `@capacitor/*`（screen-orientation, splash-screen, status-bar 等）を整理。
- **Lint**: `eslint.config.js` を調整（React Hooks ルール等）。
- **その他**: `.gitignore` 追補、`index.html` の整理、`.continue/` のエディタ設定。

## 影響・検証

- ローカルで `npm run test:run`（71件合格）・`tsc -b`・`eslint` がグリーン。
- CIにより以降のPRで回帰を自動検出。

## 補足

`employees.csv`（リポジトリ直下）はLTCアプリと無関係に見えたため、本コミットには**含めていない**（未追跡のまま）。必要なら別途追加のこと。
